# ========================================
# Ghost Protocol — 프로덕션 환경 변수
# ========================================
# 이 파일은 프로덕션 배포 시 필요한 환경 변수 템플릿입니다.
# 실제 값을 채운 후 .env.production으로 복사하여 사용하세요.
# ⚠️ 절대 실제 비밀키를 커밋하지 마세요!

# === 실행 환경 ===
NODE_ENV=production

# === Monad 블록체인 ===
# 테스트넷
MONAD_RPC_URL=https://testnet.monad.xyz/v1
MONAD_CHAIN_ID=10143
# 메인넷 (프로덕션 출시 시 아래 값으로 교체)
# MONAD_RPC_URL=https://mainnet.monad.xyz/v1
# MONAD_CHAIN_ID=1
ARENA_MANAGER_PRIVATE_KEY=           # 서버 전용 — Vercel에 설정하지 말 것!

# === 스마트 컨트랙트 주소 (배포 후 채우기) ===
GHOST_ARENA_ADDRESS=0x...
WAGER_POOL_ADDRESS=0x...
SURVIVAL_BET_ADDRESS=0x...

# === 백엔드 서버 (Railway) ===
PORT=3001                            # Railway가 자동 주입
REDIS_URL=                           # Railway Redis Add-on이 자동 주입
CORS_ORIGIN=https://ghost-protocol.vercel.app

# === AI ===
CLAUDE_API_KEY=                      # Ghost AI Tier 4+ LLM 전략용
CLAUDE_MODEL=claude-haiku-4-5-20251001

# === IPFS (Pinata) ===
PINATA_API_KEY=
PINATA_SECRET_KEY=

# === 프론트엔드 (Vercel 환경 변수로 설정) ===
VITE_API_URL=https://your-railway-app.up.railway.app/api/v1
VITE_WS_URL=wss://your-railway-app.up.railway.app
# 테스트넷
VITE_MONAD_RPC_URL=https://testnet.monad.xyz/v1
# 메인넷 (프로덕션 출시 시 아래 값으로 교체)
# VITE_MONAD_RPC_URL=https://mainnet.monad.xyz/v1
VITE_GHOST_ARENA_ADDRESS=0x...
VITE_WAGER_POOL_ADDRESS=0x...
VITE_SURVIVAL_BET_ADDRESS=0x...

# === 컨트랙트 검증 (선택) ===
MONAD_EXPLORER_API_KEY=
